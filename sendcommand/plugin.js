// Generated by CoffeeScript 1.6.3
(function() {
  define(['odo/infra/hub'], function(hub) {
    return {
      init: function(app) {
        return app.post('/sendcommand/:command', function(req, res) {
          if (req.user == null) {
            res.send(403, 'authentication required');
            return;
          }
          req.body.by = req.user.id;
          hub.send({
            command: req.params.command,
            payload: req.body
          });
          return res.send('Ok');
        });
      }
    };
  });

}).call(this);
